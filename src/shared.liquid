{% comment %}
The hosted Python transform consumes these custom settings without exposing
their values in rendered markup: fr24_api_token, flightaware_api_key,
provider_order, max_age_minutes, location_label, map_up_bearing_deg.
{% endcomment %}
